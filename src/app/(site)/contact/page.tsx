import Container from "@/components/Container";

export default function WorkPage() {
  return (
    <Container>
      <section className="py-16">
        <h1 className="font-[var(--font-serif)] text-5xl tracking-tight">Work</h1>
        <p className="mt-4 text-[rgb(var(--muted))]">Coming next: case studies.</p>
      </section>
    </Container>
  );
}
